# 보스 페이즈 시스템 가이드 (다중 페이즈 보스전)

## ?? 개요
보스가 체력에 따라 여러 페이즈로 변화하며, 각 페이즈마다 다른 배경, BGM, 특수효과를 가질 수 있는 시스템입니다.

## ?? 동작 방식

### 스테이지 8 (WWE 챔피언) 예시
```
체력 100% ~ 50%: 페이즈 1
├─ 배경: Sprites/BG/Chapter2/2_4_1
├─ BGM: Audio/BGM/Chapter2/Boss (유지)
└─ 효과: Effects/Chapter2/BossAura

체력 50% 이하: 페이즈 2 ★ 각성!
├─ 배경: Sprites/BG/Chapter2/2_4_2 (변경!)
├─ BGM: Audio/BGM/Chapter2/BossPhase2 (변경!)
└─ 효과: Effects/Chapter2/BossAuraPhase2 (강화!)
```

## ??? 파일 구조

### 1. YaCht_PhaseData.cs (신규)
페이즈별 데이터 구조체
```csharp
public struct YaCht_PhaseData
{
    public int m_phaseNumber;                   // 페이즈 번호
    public float m_healthThreshold;             // 체력 임계값 (0~1)
    public string m_backgroundResourcePath;     // 페이즈별 배경
    public string m_bgmResourcePath;            // 페이즈별 BGM (선택)
    public string m_specialEffectResourcePath;  // 페이즈별 효과 (선택)
    public string m_phaseDescription;           // 페이즈 설명
}
```

### 2. YaCht_StageData.cs (수정)
페이즈 배열 추가
```csharp
public struct YaCht_StageData
{
    // ...기존 필드...
    public YaCht_PhaseData[] m_phases;  // 페이즈 배열 (null이면 단일 페이즈)
    
    // 다중 페이즈인지 확인
    public bool HasMultiplePhases => m_phases != null && m_phases.Length > 0;
    
    // 체력 퍼센트로 현재 페이즈 가져오기
    public YaCht_PhaseData? GetPhaseByHealthPercent(float healthPercent);
}
```

### 3. YaCht_StageManager.cs (수정)
페이즈 관리 기능 추가
```csharp
public int CurrentPhase { get; private set; } = 1;
public event Action<int, YaCht_PhaseData> OnPhaseChanged;

private void CheckPhaseTransition(float previousHealth, float currentHealth);
public YaCht_PhaseData? GetCurrentPhaseData();
public bool IsMultiPhaseStage();
```

### 4. YaCht_GameScene.cs (수정)
페이즈 전환 시 리소스 자동 변경
```csharp
private void OnPhaseChanged(int phaseNumber, YaCht_PhaseData phaseData)
{
    // 배경 변경
    LoadBackgroundFromPath(phaseData.m_backgroundResourcePath);
    
    // BGM 변경 (옵션)
    if (!string.IsNullOrEmpty(phaseData.m_bgmResourcePath))
        PlayBGMFromPath(phaseData.m_bgmResourcePath);
    
    // 특수 효과 변경 (옵션)
    if (!string.IsNullOrEmpty(phaseData.m_specialEffectResourcePath))
        SpawnSpecialEffectFromPath(phaseData.m_specialEffectResourcePath);
}
```

## ?? 스테이지 8 설정 예시

### YaCht_StageDatabase.cs
```csharp
// 스테이지 8: WWE 챔피언 - 2페이즈 보스전
s_stageDataMap.Add(8, new YaCht_StageData(
    stageNumber: 8,
    chapterNumber: 2,
    backgroundPath: "Sprites/BG/Chapter2/2_4_1",  // 초기 배경 (페이즈 1)
    bgmPath: "Audio/BGM/Chapter2/Boss",
    isBoss: true,
    description: "WWE 챔피언 결전",
    effectPath: "Effects/Chapter2/BossAura",
    phases: new YaCht_PhaseData[]
    {
        // 페이즈 1: 체력 100% ~ 50%
        new YaCht_PhaseData(
            phaseNumber: 1,
            healthThreshold: 1.0f,  // 100%
            backgroundPath: "Sprites/BG/Chapter2/2_4_1",
            description: "WWE 챔피언의 첫 번째 형태",
            bgmPath: "",  // 빈 문자열 = BGM 변경 안 함
            effectPath: "Effects/Chapter2/BossAura"
        ),
        
        // 페이즈 2: 체력 50% 이하 ★ 각성!
        new YaCht_PhaseData(
            phaseNumber: 2,
            healthThreshold: 0.5f,  // 50%
            backgroundPath: "Sprites/BG/Chapter2/2_4_2",  // 변경!
            description: "WWE 챔피언의 각성 형태!",
            bgmPath: "Audio/BGM/Chapter2/BossPhase2",  // 2페이즈 전용 BGM
            effectPath: "Effects/Chapter2/BossAuraPhase2"  // 강화된 효과
        )
    }
));
```

## ?? 리소스 폴더 구조 (권장)

```
Resources/
├── Sprites/
│   └── BG/
│       └── Chapter2/
│           ├── 2_4_1.png        # 페이즈 1 배경
│           └── 2_4_2.png        # 페이즈 2 배경 (더 어둡거나 붉은색)
│
├── Audio/
│   └── BGM/
│       └── Chapter2/
│           ├── Boss.mp3         # 페이즈 1 BGM
│           └── BossPhase2.mp3  # 페이즈 2 BGM (더 격렬한 음악)
│
└── Effects/
    └── Chapter2/
        ├── BossAura.prefab          # 페이즈 1 효과
        └── BossAuraPhase2.prefab    # 페이즈 2 효과 (더 강렬한 파티클)
```

## ?? 다른 스테이지에 적용하기

### 3페이즈 보스 예시 (스테이지 12)
```csharp
s_stageDataMap.Add(12, new YaCht_StageData(
    stageNumber: 12,
    chapterNumber: 3,
    backgroundPath: "Sprites/BG/Chapter3/3_4_1",
    bgmPath: "Audio/BGM/Chapter3/Boss",
    isBoss: true,
    description: "최종 챔피언 결전",
    effectPath: "Effects/Chapter3/BossAura",
    phases: new YaCht_PhaseData[]
    {
        // 페이즈 1: 체력 100% ~ 66%
        new YaCht_PhaseData(
            phaseNumber: 1,
            healthThreshold: 1.0f,
            backgroundPath: "Sprites/BG/Chapter3/3_4_1",
            description: "최종 보스의 기본 형태"
        ),
        
        // 페이즈 2: 체력 66% ~ 33%
        new YaCht_PhaseData(
            phaseNumber: 2,
            healthThreshold: 0.66f,
            backgroundPath: "Sprites/BG/Chapter3/3_4_2",
            description: "최종 보스의 강화 형태",
            bgmPath: "Audio/BGM/Chapter3/BossPhase2"
        ),
        
        // 페이즈 3: 체력 33% 이하 ★ 최종 형태!
        new YaCht_PhaseData(
            phaseNumber: 3,
            healthThreshold: 0.33f,
            backgroundPath: "Sprites/BG/Chapter3/3_4_3",
            description: "최종 보스의 진정한 힘!",
            bgmPath: "Audio/BGM/Chapter3/BossPhase3",
            effectPath: "Effects/Chapter3/BossAuraPhase3"
        )
    }
));
```

## ?? 페이즈 전환 연출 아이디어

### 1. 배경 변화
- **페이즈 1**: 일반 링 (밝은 조명)
- **페이즈 2**: 어두워진 링 (붉은 조명, 관객 흥분)

### 2. BGM 변화
- **페이즈 1**: 긴장감 있는 보스 테마
- **페이즈 2**: 더 빠르고 격렬한 록 음악

### 3. 특수 효과
- **페이즈 1**: 기본 오라 (파란색)
- **페이즈 2**: 강렬한 오라 (붉은색 + 번개 효과)

### 4. 추가 연출 (선택)
```csharp
// YaCht_GameScene에서 페이즈 전환 시 추가 연출
private void OnPhaseChanged(int phaseNumber, YaCht_PhaseData phaseData)
{
    // 기본 리소스 변경
    LoadBackgroundFromPath(phaseData.m_backgroundResourcePath);
    
    // 페이즈 2 진입 시 특별 연출
    if (phaseNumber == 2)
    {
        // 화면 흔들림
        StartCoroutine(CameraShake(1.0f, 0.5f));
        
        // 경고 메시지
        ShowWarningText("★ 보스가 각성했습니다! ★");
        
        // 카메라 줌인
        Camera.main.orthographicSize = 4.5f;
    }
    
    // BGM 페이드 전환 (부드럽게)
    if (!string.IsNullOrEmpty(phaseData.m_bgmResourcePath))
    {
        StartCoroutine(FadeBGMTransition(phaseData.m_bgmResourcePath));
    }
}
```

## ?? 고급 활용

### 1. 페이즈별 AI 패턴 변경
```csharp
// YaCht_Enemy에 추가
public void OnPhaseChanged(int phaseNumber)
{
    switch (phaseNumber)
    {
        case 1:
            attackSpeed = 1.0f;
            break;
        case 2:
            attackSpeed = 1.5f;  // 더 빠른 공격
            enableSpecialAttack = true;  // 특수 공격 해금
            break;
    }
}
```

### 2. 페이즈별 체력바 색상 변경
```csharp
// YaCht_Enemy의 UpdateHealthBar()
private void UpdateHealthBar()
{
    // ...기존 코드...
    
    // 페이즈별 색상
    if (YaCht_GameManager.StageManager.CurrentPhase == 2)
    {
        m_hpBarFillImage.color = Color.red;  // 페이즈 2는 빨간색
    }
}
```

### 3. 페이즈 전환 UI 알림
```csharp
// UI에 페이즈 전환 알림 표시
[SerializeField] private TextMeshProUGUI phaseWarningText;

private void OnPhaseChanged(int phaseNumber, YaCht_PhaseData phaseData)
{
    StartCoroutine(ShowPhaseWarning(phaseData.m_phaseDescription));
}

private IEnumerator ShowPhaseWarning(string message)
{
    phaseWarningText.text = message;
    phaseWarningText.gameObject.SetActive(true);
    
    // 3초 동안 표시
    yield return new WaitForSeconds(3f);
    
    phaseWarningText.gameObject.SetActive(false);
}
```

## ?? 주의사항

### 1. healthThreshold 설정
- **반드시 내림차순**으로 설정해야 합니다
- ? 올바른 예: `1.0f, 0.5f, 0.25f`
- ? 잘못된 예: `0.5f, 1.0f` (동작 안 함)

### 2. 선택적 리소스
- `bgmPath`, `effectPath`를 빈 문자열(`""`)로 설정하면 변경하지 않습니다
- 페이즈 1은 스테이지의 기본 리소스를 사용할 수 있습니다

### 3. 리소스 파일 준비
- 모든 페이즈별 리소스를 미리 준비해야 합니다
- 파일이 없으면 Warning 로그만 출력되고 게임은 계속 진행됩니다

## ?? 디버깅

### 콘솔 로그 확인
```
[StageManager] 스테이지 8 로드: WWE 챔피언
[StageManager] 다중 페이즈 스테이지: 2개 페이즈
[StageManager] WWE 챔피언에게 50 데미지! 남은 체력: 2500/5000
[StageManager] ★ 페이즈 전환! Phase 2 ★
[StageManager] WWE 챔피언의 각성 형태!
[GameScene] 페이즈 2 전환: WWE 챔피언의 각성 형태!
[GameScene] 배경 로드 완료: Sprites/BG/Chapter2/2_4_2
[GameScene] BGM 재생 시작: Audio/BGM/Chapter2/BossPhase2
```

### 현재 페이즈 확인
```csharp
// 스크립트에서 확인
int currentPhase = YaCht_GameManager.StageManager.CurrentPhase;
Debug.Log($"현재 페이즈: {currentPhase}");

// 현재 페이즈 데이터 가져오기
YaCht_PhaseData? phaseData = YaCht_GameManager.StageManager.GetCurrentPhaseData();
if (phaseData.HasValue)
{
    Debug.Log($"페이즈 설명: {phaseData.Value.m_phaseDescription}");
}
```

## ?? 테스트 시나리오

### 1. 단일 페이즈 스테이지 (스테이지 1-7, 9-11)
```
? 페이즈 시스템이 동작하지 않음
? 기본 배경/BGM 유지
? 페이즈 전환 이벤트 발생하지 않음
```

### 2. 2페이즈 보스 (스테이지 8)
```
? 체력 100% ~ 51%: 페이즈 1
? 체력 50% 이하: 페이즈 2로 전환
? 배경이 2_4_1 → 2_4_2로 변경
? BGM 변경 (설정한 경우)
? 특수 효과 변경 (설정한 경우)
```

### 3. 3페이즈 보스 (스테이지 12 - 확장 가능)
```
? 체력 100% ~ 67%: 페이즈 1
? 체력 66% ~ 34%: 페이즈 2로 전환
? 체력 33% 이하: 페이즈 3으로 전환
? 각 페이즈마다 다른 리소스
```

## ?? 완성 효과

플레이어가 보스의 체력을 50% 이하로 깎으면:
1. ?? "WWE 챔피언의 각성 형태!" 메시지 출력
2. ?? 배경이 더 어둡고 극적으로 변경
3. ?? BGM이 더 격렬하게 변경
4. ? 특수 효과가 더 강렬하게 변경
5. ?? 플레이어에게 긴장감 부여!

이제 페이즈별로 다양한 연출과 난이도 조절이 가능합니다! ???
