# 카드 등급 표시 시스템 가이드

## ?? 개요
카드에 S/A/B/C/D 등급을 시각적으로 표시하는 시스템입니다.

---

## ?? 등급별 색상

| 등급 | 색상 | RGB | 의미 |
|------|------|-----|------|
| **S** | ?? 금색 | (255, 214, 0) | 최상급 |
| **A** | ?? 보라색 | (204, 51, 204) | 상급 |
| **B** | ?? 파란색 | (51, 128, 255) | 중상급 |
| **C** | ?? 초록색 | (51, 204, 51) | 중급 |
| **D** | ? 회색 | (179, 179, 179) | 일반 |

---

## ?? 구현 코드

### YaCht_WWECard.cs

#### 1. 필드 추가
```csharp
public class YaCht_WWECard : MonoBehaviour
{
    // 기존 필드들...
    public TextMeshProUGUI m_cardRarity;      // 등급 표시 텍스트
    public Image m_rarityBackground;           // 등급 배경 (선택사항)
}
```

#### 2. 등급 표시 메서드
```csharp
/// <summary>
/// 등급 표시 업데이트
/// </summary>
private void UpdateRarityDisplay()
{
    if (m_cardRarity != null)
    {
        m_cardRarity.text = GetRarityText(m_cardData.m_rarity);
    }
    
    if (m_rarityBackground != null)
    {
        m_rarityBackground.color = GetRarityColor(m_cardData.m_rarity);
    }
}

/// <summary>
/// 등급 텍스트 반환
/// </summary>
private string GetRarityText(YaCht_CardRarity rarity)
{
    switch (rarity)
    {
        case YaCht_CardRarity.S: return "S";
        case YaCht_CardRarity.A: return "A";
        case YaCht_CardRarity.B: return "B";
        case YaCht_CardRarity.C: return "C";
        case YaCht_CardRarity.D: return "D";
        default: return "?";
    }
}

/// <summary>
/// 등급별 색상 반환
/// </summary>
private Color GetRarityColor(YaCht_CardRarity rarity)
{
    switch (rarity)
    {
        case YaCht_CardRarity.S:
            return new Color(1f, 0.84f, 0f);      // 금색
        case YaCht_CardRarity.A:
            return new Color(0.8f, 0.2f, 0.8f);   // 보라색
        case YaCht_CardRarity.B:
            return new Color(0.2f, 0.5f, 1f);     // 파란색
        case YaCht_CardRarity.C:
            return new Color(0.2f, 0.8f, 0.2f);   // 초록색
        case YaCht_CardRarity.D:
            return new Color(0.7f, 0.7f, 0.7f);   // 회색
        default:
            return Color.white;
    }
}
```

#### 3. Init/UpdateCardData에서 호출
```csharp
public void Init(YaCht_CardData _cardData, bool isPreviewCard = false)
{
    // ...기존 코드...
    
    // 등급 표시 업데이트
    UpdateRarityDisplay();
}

public void UpdateCardData(YaCht_CardData _cardData)
{
    // ...기존 코드...
    
    // 등급 표시 업데이트
    UpdateRarityDisplay();
}
```

---

## ?? Unity 설정 방법

### 1. 카드 프리팹 수정

#### 옵션 A: 텍스트만 표시 (간단)
```
Card (GameObject)
├── Canvas
│   ├── CardName (TextMeshProUGUI)
│   ├── CardDescr (TextMeshProUGUI)
│   ├── CardDamage (TextMeshProUGUI)
│   └── CardRarity (TextMeshProUGUI) ← 새로 추가
```

**CardRarity 설정:**
- Position: 카드 오른쪽 상단
- Font Size: 24-32
- Alignment: Center
- Color: White (코드에서 자동 변경)

#### 옵션 B: 배경 + 텍스트 (고급)
```
Card (GameObject)
├── Canvas
│   ├── CardName (TextMeshProUGUI)
│   ├── CardDescr (TextMeshProUGUI)
│   ├── CardDamage (TextMeshProUGUI)
│   └── RarityPanel (GameObject)
│       ├── RarityBackground (Image) ← 배경
│       └── CardRarity (TextMeshProUGUI) ← 텍스트
```

**RarityPanel 설정:**
- Position: 오른쪽 상단
- Size: 40x40

**RarityBackground 설정:**
- Image Type: Sliced
- Color: White (코드에서 자동 변경)
- Alpha: 0.8-1.0

**CardRarity 설정:**
- Font Size: 24
- Alignment: Center
- Color: White

---

## ?? 레이아웃 예시

### 카드 UI 구조

```
┌─────────────────────┐
│ [S] ← 등급 배지     │
│                     │
│   카드 이름         │
│                     │
│   설명 텍스트       │
│                     │
│   데미지: 20        │
└─────────────────────┘
```

### 권장 위치

#### 1. 오른쪽 상단 (추천)
```
Position: (100, 150, 0)
Anchor: Top Right
Pivot: (1, 1)
```

#### 2. 왼쪽 상단
```
Position: (-100, 150, 0)
Anchor: Top Left
Pivot: (0, 1)
```

#### 3. 중앙 상단
```
Position: (0, 150, 0)
Anchor: Top
Pivot: (0.5, 1)
```

---

## ?? 스타일 가이드

### 1. 미니멀 스타일
```
배경: 없음
텍스트: 크게 (36pt)
효과: 텍스트 외곽선 (Outline)
```

### 2. 배지 스타일 (추천)
```
배경: 둥근 사각형 (20x20)
텍스트: 중간 (24pt)
효과: 그림자 (Shadow)
```

### 3. 고급 스타일
```
배경: 그라데이션
텍스트: 금속 느낌
효과: Glow + Outline
```

---

## ?? 커스터마이징

### 1. 색상 변경

#### 무지개 스타일
```csharp
case YaCht_CardRarity.S:
    return new Color(1f, 0f, 0f);      // 빨강
case YaCht_CardRarity.A:
    return new Color(1f, 0.5f, 0f);    // 주황
case YaCht_CardRarity.B:
    return new Color(1f, 1f, 0f);      // 노랑
case YaCht_CardRarity.C:
    return new Color(0f, 1f, 0f);      // 초록
case YaCht_CardRarity.D:
    return new Color(0f, 0f, 1f);      // 파랑
```

#### 모노톤 스타일
```csharp
case YaCht_CardRarity.S:
    return new Color(0.2f, 0.2f, 0.2f);  // 검정
case YaCht_CardRarity.A:
    return new Color(0.4f, 0.4f, 0.4f);  // 진한 회색
case YaCht_CardRarity.B:
    return new Color(0.6f, 0.6f, 0.6f);  // 회색
case YaCht_CardRarity.C:
    return new Color(0.8f, 0.8f, 0.8f);  // 밝은 회색
case YaCht_CardRarity.D:
    return new Color(1f, 1f, 1f);        // 흰색
```

### 2. 텍스트 스타일

#### 아이콘 사용
```csharp
case YaCht_CardRarity.S: return "★";
case YaCht_CardRarity.A: return "◆";
case YaCht_CardRarity.B: return "●";
case YaCht_CardRarity.C: return "▲";
case YaCht_CardRarity.D: return "■";
```

#### 전체 이름
```csharp
case YaCht_CardRarity.S: return "SUPER";
case YaCht_CardRarity.A: return "ACE";
case YaCht_CardRarity.B: return "BEST";
case YaCht_CardRarity.C: return "COMMON";
case YaCht_CardRarity.D: return "DEFAULT";
```

### 3. 애니메이션 추가

```csharp
private void UpdateRarityDisplay()
{
    // ...기존 코드...
    
    // S등급은 빛나는 효과
    if (m_cardData.m_rarity == YaCht_CardRarity.S && m_rarityBackground != null)
    {
        StartCoroutine(GlowEffect());
    }
}

private IEnumerator GlowEffect()
{
    float time = 0f;
    while (true)
    {
        time += Time.deltaTime;
        float alpha = (Mathf.Sin(time * 3f) + 1f) * 0.5f;
        m_rarityBackground.color = new Color(1f, 0.84f, 0f, alpha);
        yield return null;
    }
}
```

---

## ?? Inspector 설정

### YaCht_WWECard 컴포넌트

```
[기존 필드]
- m_cardCanvas
- m_cardImage
- m_cardDescr
- m_cardName
- m_cardDamage

[새 필드]
- m_cardRarity: TextMeshProUGUI ← CardRarity 오브젝트 드래그
- m_rarityBackground: Image ← RarityBackground 오브젝트 드래그 (선택사항)
```

---

## ?? 사용 예시

### 카드 생성 시 자동 적용
```csharp
// 카드 생성
GameObject cardObj = Instantiate(m_cardPrefab);
YaCht_WWECard card = cardObj.GetComponent<YaCht_WWECard>();

// 카드 데이터 설정 (등급 자동 표시됨)
card.Init(YaCht_CardDatabase.AA);  // S등급 → 금색 "S" 표시
```

### 등급별 표시 결과
```
AA (S등급)          → [S] 금색
RKO (S등급)         → [S] 금색
파이브 너클 셔플 (A) → [A] 보라색
숄더 태클 (B)       → [B] 파란색
아이리쉬 윕 (C)     → [C] 초록색
박치기 (D)          → [D] 회색
```

---

## ??? 트러블슈팅

### Q1: 등급이 표시되지 않아요
**A**: Inspector를 확인하세요:
1. `m_cardRarity` 필드가 연결되어 있는지 확인
2. TextMeshProUGUI 컴포넌트가 있는지 확인
3. Canvas가 활성화되어 있는지 확인

### Q2: 색상이 변경되지 않아요
**A**: 
1. `m_rarityBackground`가 연결되어 있는지 확인
2. Image 컴포넌트의 Color가 White(255,255,255)로 설정되어 있는지 확인
3. Material이 기본값인지 확인

### Q3: 등급이 겹쳐 보여요
**A**: UI 레이아웃을 조정하세요:
```
RectTransform:
- Anchor Presets: Top Right
- Pivot: (1, 1)
- Position: (-10, -10, 0) ← 카드 안쪽으로 10px
```

### Q4: 텍스트가 안 보여요
**A**: 
1. Font Size를 키우세요 (24 이상)
2. Outline 효과를 추가하세요
3. Canvas Sorting Order 확인

---

## ? 체크리스트

카드 프리팹 설정 시:

- [ ] CardRarity TextMeshProUGUI 오브젝트 생성
- [ ] 위치: 오른쪽 상단 설정
- [ ] Font Size: 24-32 설정
- [ ] YaCht_WWECard 컴포넌트에 연결
- [ ] (선택) RarityBackground Image 오브젝트 생성
- [ ] (선택) 배경 크기: 40x40 설정
- [ ] (선택) YaCht_WWECard 컴포넌트에 연결
- [ ] 테스트: S등급 카드 → 금색 표시
- [ ] 테스트: A등급 카드 → 보라색 표시
- [ ] 테스트: D등급 카드 → 회색 표시

---

## ?? 등급 시각화 효과

### Before (기존)
```
┌─────────────┐
│ AA          │
│ 20 데미지   │
└─────────────┘
→ 등급을 알 수 없음
```

### After (개선)
```
┌─────────────┐
│ AA      [S] │ ← 금색
│ 20 데미지   │
└─────────────┘
→ S등급임을 한눈에 알 수 있음
```

---

## ?? 활용 팁

### 1. 정렬 기능
```csharp
// 등급순 정렬
List<YaCht_CardData> sortedCards = playerDeck
    .OrderBy(card => card.m_rarity)
    .ToList();
```

### 2. 필터링
```csharp
// S등급만 표시
List<YaCht_CardData> sRankCards = playerDeck
    .Where(card => card.m_rarity == YaCht_CardRarity.S)
    .ToList();
```

### 3. 통계 표시
```csharp
// 덱 구성 통계
Dictionary<YaCht_CardRarity, int> rarityCount = new Dictionary<YaCht_CardRarity, int>();
foreach (var card in playerDeck)
{
    if (!rarityCount.ContainsKey(card.m_rarity))
        rarityCount[card.m_rarity] = 0;
    rarityCount[card.m_rarity]++;
}

// 출력: S: 2장, A: 3장, B: 3장, C: 2장
```

---

## ?? 요약

**카드 등급 표시 시스템**은 플레이어가 카드의 희귀도를 한눈에 파악할 수 있게 해줍니다.

### 핵심 기능:
- ?? **등급별 색상**: S(금), A(보라), B(파랑), C(초록), D(회색)
- ?? **자동 표시**: Init/UpdateCardData 시 자동 업데이트
- ?? **유연한 커스터마이징**: 색상, 위치, 스타일 변경 가능
- ?? **옵션**: 텍스트만 or 배경+텍스트

이제 카드의 등급을 시각적으로 확인하세요! ??
